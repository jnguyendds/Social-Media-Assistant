(function(root){
  const D=root.SignalDiagnostics||(typeof require!=='undefined'?require('./signal-diagnostics.js'):null);
  function redact(value){return D.redact(value);}
  function diagnostics(base){return Object.assign(D.create({promptVersion:base.promptVersion,model:base.model,provider:'anthropic'}),{parsingResult:'pending',validationResult:'pending',retryCount:0,legacyFallbackUsed:false,diversityCheckFailures:[],unsupportedOperations:[],errors:[]});}
  function classified(category,message,diag){const e=new Error(message);e.category=category;e.diagnostics=diag;if(diag){diag.errorCategory=category;diag.userMessage=message;}return e;}
  function validationMessage(errors){const detail=D.summarizeFailures(errors);return detail?`Signal received AI JSON, but it failed validation: ${detail}`:'Signal received AI JSON, but it failed schema validation.';}
  function extractText(data){return((data&&data.content)||[]).filter(b=>b.type==='text').map(b=>b.text).join('').trim();}
  async function send(fetchImpl,args,system,userContent,diag){if(!args.apiKey)throw classified('missing-api-key','Add your Anthropic API key in Settings → AI Providers before optimizing.',diag);const started=Date.now();let r;try{r=await fetchImpl('https://api.anthropic.com/v1/messages',{method:'POST',headers:{'content-type':'application/json','x-api-key':args.apiKey,'anthropic-version':'2023-06-01','anthropic-dangerous-direct-browser-access':'true'},body:JSON.stringify({model:args.model,max_tokens:4000,system,messages:[{role:'user',content:userContent}]})});}catch(e){throw classified('network','Could not reach Anthropic. Check your connection and try again.',diag);}if(diag)diag.httpStatus=r.status;if(r.status===401||r.status===403)throw classified('invalid-api-key','Anthropic rejected this API key. Open Settings → AI Providers and check it.',diag);if(!r.ok){let m='';try{m=(await r.json()).error?.message||'';}catch(e){}const limited=r.status===429&&/rate|limit|credit/i.test(m);throw classified(limited?'rate-or-quota':'anthropic-http',limited?`Anthropic reported request limiting: ${m}`:`Anthropic returned HTTP ${r.status}. ${m}`.trim(),diag);}const data=await r.json();const text=extractText(data);if(diag){diag.requestDurationMs=Date.now()-started;diag.rawResponse=text;}return{text,duration:Date.now()-started,data};}
  function isPlainObjectForCategory(v){return v!==null&&typeof v==='object'&&!Array.isArray(v);}
  function unsupported(result,context){const supported=new Set((context.supportedLocalRendererOperations||[]));const out=[];(result.options||[]).forEach(o=>(o.localAdjustments||[]).forEach(a=>{if(a.operation&&!supported.has(a.operation))out.push(a.operation);}));return Array.from(new Set(out));}
  async function optimize(args){
    const fetchImpl=args.fetchImpl||root.fetch;const builder=(typeof module!=='undefined'&&module.exports)?require('./signal-prompt-builder.js'):root.SignalPromptBuilder;const contract=(typeof module!=='undefined'&&module.exports)?require('./signal-contract.js'):root.SignalContract;
    const req=args.carousel?builder.buildCarouselAnthropicRequest(args):builder.buildAnthropicRequest(args);const diag=diagnostics({promptVersion:req.promptVersion,model:args.model});
    D.stage(diag,'prompt construction','ok',{promptVersion:req.promptVersion});
    const userContent=[...(args.mediaContent||[]),{type:'text',text:req.userText}];
    let raw='';
    try{D.stage(diag,'anthropic request','started');const first=await send(fetchImpl,args,req.system,userContent,diag);D.stage(diag,'http response','ok',{httpStatus:diag.httpStatus});raw=first.text;diag.requestDurationMs=first.duration;try{D.stage(diag,'JSON extraction','started');const result=contract.parseStrictNativeV2(raw,req.context);diag.parsedResponse=result;diag.parsingResult='native V2';diag.validationResult='valid';diag.validationStatus='valid';diag.unsupportedOperations=unsupported(result,req.context);result.diagnostics=diag;return{result,diagnostics:diag};}catch(e){diag.errors.push({category:e.category||'schema-validation',detail:redact(e.validationErrors||e.message)});if((e.validationErrors||[]).some(x=>String(x).includes('duplicate')||String(x).includes('similar')))diag.diversityCheckFailures=e.validationErrors;}
      diag.validationFailures=(diag.validationFailures||[]).concat((diag.errors||[]));diag.retryCount=1;D.stage(diag,'repair attempt','started');const repairText=`Repair the previous response into corrected native Signal V2 JSON only. Do not include Markdown or prose. Use the canonical SignalContract schemaVersion 2.0 shape and request context: ${redact(req.context)}. Validation errors: ${redact(diag.errors)}. Schema shape rules: ${redact(contract.responseShape||{})}. Required repairs: subject.boundingBoxes must be an array of {x,y,width,height,units,label?} objects (convert [x,y,width,height] arrays or {x,y,w,h} objects if present); captions must be an array of {text,tone,callToAction} objects even for one caption; hashtags.recommended must be an array of hashtag strings. Previous response redacted summary: ${redact(raw)}`;
      const repaired=await send(fetchImpl,args,req.system,[{type:'text',text:repairText}],diag);try{const result=contract.parseStrictNativeV2(repaired.text,req.context);diag.parsedResponse=result;diag.parsingResult='repaired native V2';diag.validationResult='valid';diag.validationStatus='valid';diag.repairAttemptResult='success';diag.unsupportedOperations=unsupported(result,req.context);result.diagnostics=diag;return{result,diagnostics:diag};}catch(e2){diag.repairAttemptResult='failed';diag.errors.push({category:e2.category||'schema-validation',detail:redact(e2.validationErrors||e2.message)});diag.validationFailures=(diag.validationFailures||[]).concat(e2.validationErrors||[e2.message]);}
      try{const parsed=contract.parseOptimizationResponse(raw);if(parsed&&parsed.schemaVersion!=='2.0'){const adapted=contract.parseValidateNormalizeOptimizationResult(raw);diag.parsingResult='legacy adapted';diag.validationResult='valid';diag.legacyFallbackUsed=true;adapted.diagnostics=diag;return{result:adapted,diagnostics:diag};}}catch(e3){diag.errors.push(redact(e3.message));}
      diag.parsingResult='failed';diag.validationResult='invalid';diag.validationStatus='invalid';const failures=diag.validationFailures&&diag.validationFailures.length?diag.validationFailures:diag.errors;const categories=(diag.errors||[]).map(x=>isPlainObjectForCategory(x)?x.category:null).filter(Boolean);const lastCategory=categories[categories.length-1]||(failures.some(x=>/Unexpected token|Unexpected end|JSON|object without prose|incomplete/i.test(String(x)))?'invalid-json':'schema-validation');const category=lastCategory==='invalid-json'?'invalid-json':lastCategory==='unsupported-schema'?'unsupported-schema':'schema-validation';const messages={
        'invalid-json':'Anthropic returned text that was not parseable JSON after one repair attempt. Please retry.',
        'unsupported-schema':'Anthropic returned valid JSON, but it was not a supported Signal response shape after one repair attempt.',
        'schema-validation':validationMessage(failures)
      };throw classified(category,messages[category],diag);
    }catch(e){if(!e.category&&!(e.message||'').includes('Anthropic')){e=classified('internal',e.message||'Unexpected internal optimization error.',diag);}e.diagnostics=diag;throw e;}
  }
  async function optimizeCarousel(args){return optimize({...args,carousel:true});}
  const api={optimize,optimizeCarousel,redact};if(typeof module!=='undefined'&&module.exports)module.exports=api;root.SignalAIClient=api;
})(typeof window!=='undefined'?window:globalThis);
